import type { SelectorBoxPopoverProps } from "@flowgram.ai/free-layout-editor";

/**
 * 每一个节点选中后的弹出框
 * @param param0 
 * @returns 
 */
const SelectBoxPopover: React.FC<SelectorBoxPopoverProps> = ({
    bounds,
    children,
    flowSelectConfig,
    commandRegistry
}) => {
  return (
    <div>
      SelectBoxPopover
    </div>
  );
};
export default SelectBoxPopover;